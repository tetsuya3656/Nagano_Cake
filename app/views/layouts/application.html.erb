<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="text-right">
        <a class="navbar-brand" href="/">
         <i class="fa-sharp fa-solid fa-cake-slice"></i>
        </a>

        <% if customer_signed_in? %>
          <%= link_to "マイページ", public_customer_path(current_customer), class:"btn btn-info" %>
          <%= link_to "商品一覧", public_items_path, class:"btn btn-info" %>
          <%= link_to "カート", public_cart_items_path, class:"btn btn-info" %>
          <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class:"btn btn-info" %>
        <% else %>
          <%= link_to "About", public_about_path, class:"btn btn-info" %>
          <%= link_to "商品一覧", public_items_path, class:"btn btn-info" %>
          <%= link_to "新規登録", new_customer_registration_path, class:"btn btn-info" %>
          <%= link_to "ログイン", new_customer_session_path, class:"btn btn-info" %>
        <% end %>

      </div>
    </nav>
    </header>
    <main>
    <%= yield %>
    </main>
    <footer>
      <div class = "container mt-5">
        <div class= "row">
          <div class ="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
